[package]
name = "fintech_api"
version = "0.1.0"
edition = "2018"

[dependencies]
actix-web = "4.0"
diesel = { version = "2.0", features = ["postgres"] }
dotenv = "0.15"

[dev-dependencies]
actix-web = "4.0"
diesel = { version = "2.0", features = ["postgres"] }
dotenv = "0.15"